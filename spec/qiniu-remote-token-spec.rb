require "spec_helper"

describe QiniuRemoteTocken do
  it "has a version number" do
    expect(QiniuRemoteTocken::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
